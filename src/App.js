import React, { Component } from 'react';
import HelloWorld from './components/HelloWorld';
import {
  BrowserRouter as Router,
  Route,
  Link
} from 'react-router-dom';

/**
 * App contains Base Route for the website.
 *     Create a small website with route
      - /home: Display anything.
      - /counter: counter increment on button Click.
      - /calculator: calculator app to add subtract.
           -/calculator/add: add 2 number.
           -/calculator/sub: subtract 2 number.
 */
const App = () => (
  <div>
    create App.
  </div>
)
export default App;
